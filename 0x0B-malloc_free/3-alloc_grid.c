#include "main.h"
#include <stdlib.h>

/**
 * alloc_grid - allocate a nested array
 * @width: width
 * @height: height
 * Return: pointer to array or NULL
 */
int **alloc_grid(int width, int height)
{
	int *arr[width];
	int p* = arr;

	return (p);
}
